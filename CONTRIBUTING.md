# Contributing to Codex 额度

Thanks for your interest in improving Codex 额度 ✨

## Before you start

- Read `AGENTS.md` for project principles and constraints.
- Keep changes local-first and avoid adding network calls unless requested.
- Keep the menu bar UI compact (`percent | reset`) and avoid extra noise.
- Prefer small, focused PRs.

## Development setup

```bash
cd codex-meter
scripts/build-app.sh
```

Run again after changes and test the app with:

```bash
swift test
scripts/restart.sh
```

## Code style

- Prefer SwiftUI/AppKit standard APIs.
- Keep behavior deterministic and avoid fake/mock fallback for quota values.
- Use clear, minimal labels in Chinese/English mix consistent with existing UI.

## Reporting bugs

- Reproduce with steps and expected/actual behavior.
- Mention macOS version and whether local `.codex/sessions` exists.

## Pull requests

- Keep diffs scoped.
- Add or update comments only when behavior is non-obvious.
- Make sure builds still pass locally.

## Release checklist (for maintainers)

1. Update version in `Info.plist` keys (`CFBundleShortVersionString`, `CFBundleVersion`) generated by `scripts/build-app.sh`.
2. Update `CHANGELOG.md`.
3. Tag a release (for example: `v0.1.0`).
4. Run `scripts/package-release.sh` and verify its SHA-256 file.
5. For public distribution, sign with Developer ID and notarize using a configured `notarytool` keychain profile.
